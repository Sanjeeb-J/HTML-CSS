// Home Exercise: Simple Web Calculator
// Create an HTML file with an empty <h2> block having id="result".
// Ask the user for two separate numbers using prompt(), adds them together (ensure you handle type conversion!),
// and updates the textContent of the <h2> block to show: "The sum is: [result]".

